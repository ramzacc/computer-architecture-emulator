{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check public/app.js && node --check public/components.js && node --check public/editor.js && node --check public/model.js && node --check public/renderer.js && node --check editor.test.js && node --check model.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.137.0"
  }
}
